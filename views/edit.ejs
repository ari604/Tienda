<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Bebidas</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }
        
        .container {
            max-width: 1000px;
            margin-top: 50px;
        }
        
        .table {
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        
        .table-dark {
            background-color: #343a40;
        }
        
        .table-dark th {
            color: #fff;
        }
        
        .btn {
            font-size: 1rem;
            padding: 12px 20px;
            border-radius: 5px;
        }
        
        .btn-warning {
            background-color: #ff9800;
            border-color: #ff9800;
        }
        
        .btn-warning:hover {
            background-color: #f57c00;
            border-color: #f57c00;
        }
        
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }
        
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }
        
        .btn-secondary {
            background-color: #6c757d;
            border-color: #6c757d;
        }
        
        .btn-secondary:hover {
            background-color: #5a6268;
            border-color: #545b62;
        }
        
        .text-center h1 {
            font-size: 2rem;
            color: #495057;
        }
        
        .table-responsive {
            margin-top: 20px;
        }
        
        .text-center a {
            font-size: 1.1rem;
            padding: 12px 25px;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="text-center mb-4">Lista de Bebidas</h1>

        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Código</th>
                        <th>Nombre</th>
                        <th>Marca</th>
                        <th>Precio</th>
                        <th>Categoría</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <% bebidas.forEach(bebida => { %>
                        <tr>
                            <td>
                                <%= bebida.codigo %>
                            </td>
                            <td>
                                <%= bebida.nombre %>
                            </td>
                            <td>
                                <%= bebida.marca %>
                            </td>
                            <td>$
                                <%= bebida.precio.toFixed(2) %>
                            </td>
                            <td>
                                <%= bebida.categoria %>
                            </td>
                            <td>
                                <a href="/bebidas/edit/<%= bebida._id %>" class="btn btn-warning btn-sm">Editar</a>
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
        </div>

        <div class="text-center mt-4">
            <a href="/bebidas" class="btn btn-secondary">Volver al Inicio</a>
            <a href="/bebidas/nuevo" class="btn btn-primary">Agregar Nueva Bebida</a>
        </div>
    </div>
</body>

</html>